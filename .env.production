# Configurações de Produção - CodeCraft
# Este arquivo é usado durante o build de produção

# URL da API em produção - NÃO DEFINIR para usar window.location.origin automaticamente
# VITE_API_URL deve ser omitido em produção para usar o domínio atual
# Para desenvolvimento local, use a URL local no .env

# URL da API WordPress (se aplicável)
VITE_WP_API_URL=https://cms.codecraftgenz.com.br/wp-json

# Ambiente da aplicação
VITE_APP_ENV=production

# Admin reset token (configure também em Azure App Settings)
ADMIN_RESET_TOKEN=codecraftgenz

# Mercado Pago – URLs de retorno em produção
MERCADO_PAGO_SUCCESS_URL=https://codecraftgenz.azurewebsites.net/apps/:id/compra
MERCADO_PAGO_FAILURE_URL=https://codecraftgenz.azurewebsites.net/apps/:id/compra
MERCADO_PAGO_PENDING_URL=https://codecraftgenz.azurewebsites.net/apps/:id/compra

# Webhook em produção
MERCADO_PAGO_WEBHOOK_URL=https://codecraftgenz.azurewebsites.net/api/apps/webhook

# Token de acesso (não commitar segredo aqui; use Azure App Settings)
MERCADO_PAGO_ACCESS_TOKEN=
MERCADO_PAGO_PUBLIC_KEY=
VITE_MERCADO_PAGO_PUBLIC_KEY=

# Configurações de debug (desativado em produção)
VITE_DEBUG=false

# Configurações de cache e performance
VITE_ENABLE_PWA=true
VITE_CACHE_MAX_AGE=300