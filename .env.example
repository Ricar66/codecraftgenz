# Configurações do Sistema CodeCraft
# Sistema 100% SQLite - Sem dependências externas

# Porta do servidor (padrão para desenvolvimento)
PORT=8080

# Ambiente (será definido automaticamente pelo processo de build)
# NODE_ENV será 'development' no dev server e 'production' no build

# Configurações SQLite (banco local)
SQLITE_DB_PATH=./database.sqlite

# Configurações de Segurança (opcional)
# JWT_SECRET=your-secret-key-here
# SESSION_SECRET=your-session-secret-here

# Configurações de Email (se necessário)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@example.com
# EMAIL_PASS=your-email-password

# Configurações de API Externa (se necessário)
# API_KEY=your-api-key-here
# API_URL=https://api.example.com